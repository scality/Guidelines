general:
  branches:
    ignore:
      - /^ultron\/.*/ # Ignore ultron/* branches

machine:
    node:
        version: 4.1

test:
    override:
        - npm run --silent lint
        - npm run --silent lint_md
